const locationDiv = document.querySelector(".location-div");

const p1 = (locationDiv.children[0].textContent = location.href);

const p2 = (locationDiv.children[1].textContent = location.hostname);

const p3 = (locationDiv.children[2].textContent = location.port);

const p4 = (locationDiv.children[3].textContent = location.pathname);
